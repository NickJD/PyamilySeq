title: PyamilySeq
author:
  name: NickJD
  email: nicholas@dimonaco.co.uk

# The `>` after `description:` means to ignore line-breaks until next key.
# If you want to omit the line-break after the end of text, use `>-` instead.
description: >-
  PyamilySeq

url: https://nickjd.github.io/PyamilySeq/


theme: "just-the-docs"


plugins:
  - jekyll-feed
  - jekyll-seo-tag

